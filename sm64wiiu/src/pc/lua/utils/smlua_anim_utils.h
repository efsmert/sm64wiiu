#ifndef SMLUA_ANIM_UTILS_H
#define SMLUA_ANIM_UTILS_H

// Wii U port: DynOS behavior pack code includes this header in Co-op DX. The
// full animation utility surface is not needed for loading DynOSBIN levels on
// Wii U yet, so keep this as a minimal stub to satisfy DynOS build deps.

#endif

