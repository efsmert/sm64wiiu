#ifndef CUSTOM0_H
#define CUSTOM0_H
// Wii U port: Co-op DX DynOS expects optional "custom0" actor data to exist.
// The base Wii U build does not ship with any built-in custom actors, so this
// header is intentionally empty and only satisfies include paths.
#endif

